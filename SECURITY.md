# 安全策略

## 支持范围

| 版本 | 支持 |
|---|---|
| 最新版本（当前 `v0.1.0`） | ✅ 安全更新 |
| 历史版本 | ❌ 不支持，请升级到最新版 |

## 上报漏洞

如果你发现安全漏洞，请**不要**提交公开 Issue。按以下方式私密上报：

1. 通过 GitHub 的[私密安全通告](https://github.com/lennan747/zentao-cli/security/advisories/new)（推荐）；或
2. 发送邮件至 [lennan747@gmail.com](mailto:lennan747@gmail.com)，标题注明 `[zentao-cli security]`。

请在报告中包含：

- 影响版本
- 复现步骤或概念验证
- 潜在影响与利用条件

我们会尽快确认并回复，修复后按流程发布并在变更日志中致谢（除非你要求匿名）。

## 安全承诺

- 48 小时内确认收到报告。
- 修复前不公开细节；修复发布后披露。

## 安全设计

- 密码不保存到配置文件或会话文件，不进入命令行参数与日志。
- Session Cookie 文件权限 `0o600`。
- 密码仅在登录时经无回显终端读取；无 TTY 时退化为 stdin 读取并给出警告。

## 已知限制

- 目标实例使用旧版禅道协议，登录链路依赖 `MD5(MD5(密码)+verifyRand)`（服务端接口形态所限），传输仍建议通过 HTTPS。
